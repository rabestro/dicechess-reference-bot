version = 3.11.1
maxColumn = 120
align.preset = more

runner.dialect = "Scala3"

rewrite.scala3.convertToNewSyntax = true
rewrite.rules = [RedundantBraces, RedundantParens, SortModifiers]

# Scope directory scans to git-tracked files (keeps target/ generated sources out)
project.git = true
